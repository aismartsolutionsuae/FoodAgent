# Project-specific variables only.
# Global vars (SUPABASE_URL, OPENAI_API_KEY, etc.) are set at root/.env.local

BOT_TOKEN=                    # from @BotFather
LEMONSQUEEZY_CHECKOUT_URL=    # from LS Dashboard → Products → Share (if paid)
# STRIPE_PRICE_ID=            # if using Stripe instead
# MY_PROJECT_API_KEY=         # any project-specific external API keys
